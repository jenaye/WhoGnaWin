{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tournament creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="form-control btn btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

            <a class="form-control btn btn-primary" href="{{ path('tournament_index') }}">Back to the list</a>

{% endblock %}
