<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}WhognaWin - {% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" type="text/css" href="{{ asset('css/main.css') }}">
            <link href="//cdnjs.cloudflare.com/ajax/libs/select2/3.5.4/select2.css" rel="stylesheet" />
            <link href="//cdnjs.cloudflare.com/ajax/libs/select2/3.5.4/select2-bootstrap.css" rel="stylesheet" />
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="{{ path('homepage') }}">WhoGnaWin</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor03">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('search_player_by_name_get') }}">Rechercher un joueur</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('game_index') }}">Voir tous les matchs</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('player_index',  { 'gender': 'Female'  }) }}">Liste des joueuses</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('player_index',  { 'gender': 'Male'  }) }}">Liste des joueurs</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('tournament_index') }}">Voir les tournois</a>
                    </li>
                </ul>
            </div>
        </nav>
    </head>
    <body>
    <div class="container">
        {% block body %}{% endblock %}
    </div>
        {% block javascripts %}
                            {% endblock %}
    </body>
</html>
